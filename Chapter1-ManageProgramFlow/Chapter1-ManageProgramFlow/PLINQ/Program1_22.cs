using System;
using System.Linq;

namespace Chapter1_ManageProgramFlow.PLINQ
{
    public static class Program1_22
    {
        //public static void Main()
        //{
        //    var numbers = Enumerable.Range(1, 100000000);
        //    var parallelResult = numbers.AsParallel()
        //        .Where(i => i%2 == 0)
        //        .ToArray();
        //}
    }
}
