namespace Chapter1_ManageProgramFlow.PLINQ
{
    // plinq doesn't execute in any particular order.
    public static class Program1_23
    {
        //public static void Main()
        //{
        //    var numbers = Enumerable.Range(0, 10);
        //    var parallelResult = numbers.AsParallel()
        //        .Where(i => i%2 == 0)
        //        .ToArray();

        //    foreach (var i in parallelResult)
        //    {
        //        Console.WriteLine(i);
        //    }
        //}
    }
}